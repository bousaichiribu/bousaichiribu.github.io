{
  "name": "bousaichiribu-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "node build.mjs",
    "test": "npm run build && node --test tests/static-site.test.mjs"
  }
}
